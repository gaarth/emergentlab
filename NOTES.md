# NOTES — stubs, decisions, known issues

## Contract (phase 0)
- `replay.size` is 64 in all fixtures (= `ca.GRID_SIZE`), not the 48 shown in the PRD sample.
- `seed` and `experiment_code` are present on fixture nodes (lab writes them) but are not required keys in `tests/test_contract.py`.
- `tree_big.json` orphans: `n13` (parent `n913`) and `n29` (parent `n929`). Best node is `n31` (fitness 4.0, unique max).
- `fixtures/paper_fake.md` is generated by `fixtures/make_big.py` from `tree_big.json`, so its numbers match that tree.

## 3D app (replaces viewer.html, 23 Sep 19:40–20:10)
- `app/` = React 18 + Vite 5 + R3F 8 + drei 9 + postprocessing + Theatre.js 0.7. Spec in `docs/IMPLEMENTATION_TEAMMATE.md`.
- `server.py` replaces `python -m http.server`: same static files + `/api/start` (spawns `lab.py`, or `mock_lab.py` when the
  mock toggle is on or no API key) + `/api/stop` (touches STOP). Previous `tree.json` is archived to `runs/` on each start.
- Vite proxy targets `127.0.0.1:8000`, not `localhost`: Node resolved `localhost` to `::1` while server.py listens on IPv4,
  which made `/tree.json` fail intermittently in dev.
- Dropped drei `<Environment preset>`: it downloads an HDR from a CDN and R3F suspends the whole canvas until it arrives —
  black scene offline. No runtime network assets remain.
- Theatre.js state is hand-written (`app/src/theatre-state.json`, definitionVersion 0.4.0). If Theatre rejects it we fall
  back to an empty project and a 4 s timer forces the intro to its end, so the tree can never stay hidden.
- Playwright runs Chromium with `--use-gl=swiftshader`; tests load `?nofx&nointro` for deterministic headless frames.
- The section below describes the retired `viewer.html`; kept for history.

## Viewer (V1/V2) — retired
- `viewer.html?src=<path>` points the viewer at any tree file; `&poll=<ms>` overrides the 2 s poll. `#n17` selects a node.
- D3 is loaded from `vendor/d3.min.js` first (works with Wi-Fi down) and falls back to cdnjs.
- Test hooks: `window.stepCA(rule, seed, size, steps)`, `window.__replayTimer`, `window.__frames`, `window.__panelBuilds`.
- The panel fingerprint is the whole node JSON: any change to the selected node rebuilds it once; no change means the replay keeps running.
- Until the user clicks, the viewer follows `best_node_id` (so it auto-selects the best node, and follows a new best).
- Replay parity is checked against `tests/ca_grid.py` (the spec `ca.py` code) and against the real `ca.py` when it is importable.
  As of 19:15, `ca.py` is not in the repo yet, so the test only compared against the reference copy. Rerun once Muaaz pushes.
- Fixture frame PNGs do not exist; the "no console errors" test ignores `Failed to load resource` 404s. Missing frames are hidden.
- "Selection survives a poll" clicks `n0` (not the auto-selected best `n1`) so a reset would actually be caught.

## Paper renderer (V3)
- `render_paper.py [in.md] [out.html]`, defaults `paper.md` / `paper.html` (lab.py calls it with no args).
- Image paths resolve relative to the .md folder, then cwd. Missing images are dropped with a stderr warning.
- `(n17)` / `(n3, n17)` citations become links to `viewer.html#n17`. Light academic theme (judges read on white).

## Mock backend
- `python mock_lab.py [--nodes 60] [--interval 4] [--dir .]` grows `tree.json` like lab.py (real spec CA, random proposals,
  frames, STOP file, writing_paper -> paper.html). It overwrites the root `tree.json`; `git checkout tree.json` restores the fake.
- **Proxy gaming (tell Muaaz):** under the spec fitness, `B1357/S1357` (Fredkin parity rule, copies *any* pattern) and B1 rules
  with tiny seeds like the blinker `[[1,1,1]]` scored 50–76 copies. The real loop will likely find this. Either treat it as
  a legitimate finding or exclude seeds < 4 cells / B1 rules in the prompt. The mock excludes both.

## Demo check (phase D)
- `python demo_check.py`. "Loop alive" uses log/tree.json freshness (< 60 s), not `ps` (the loop may run on Muaaz's laptop).
- Video length uses `ffprobe` if installed, else a > 1 MB size heuristic.
- `tests/live.spec.js` is the Playwright smoke against the real `tree.json`.

## Windows
- No `make` on Gaarth's machine. The Makefile targets are plain commands; run them directly (see Makefile).
